'use client';

import { useMemo, useRef, useState } from 'react';
import Link from 'next/link';

const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000';
const initialAnswers = { budget: null, brand_preference: 'no_preference', brand_other: '', display: null, storage: null, size: null, durability: null, software_longevity: null, current_phone: '', gaming: null, photography: null, refresh_rate: null, ecosystem: null, charging: null, software_ui: null, extended_set_completed: false };

const coreQuestions = [
  { key: 'budget', icon: '💰', title: 'What’s your budget?', subtitle: 'Let’s start with the practical part — what’s your budget range?', why: 'Budget is a hard filter, so every recommendation stays realistically purchasable.', options: [['15k', '₹', 'Under ₹15,000', 'Essential phones for everyday basics'], ['15_25k', '✨', '₹15,000–₹25,000', 'The value sweet spot for most buyers'], ['25_40k', '⚡', '₹25,000–₹40,000', 'Strong all-rounders with premium touches'], ['40_70k', '🚀', '₹40,000–₹70,000', 'Premium cameras, performance, and displays'], ['70k_plus', '👑', '₹70,000+', 'No-compromise flagship phones']] },
  { key: 'brand_preference', icon: '🏷️', title: 'Any brand you already trust or prefer?', subtitle: 'Choose one only if it genuinely matters; it is an optional filter.', why: 'Your chosen brand is treated as a filter. “No preference” lets the score choose freely.', options: [['Samsung', '🌌', 'Samsung', 'Galaxy phones and ecosystem'], ['Apple', '🍎', 'Apple', 'iPhone and Apple ecosystem'], ['Xiaomi', '🟠', 'Xiaomi', 'Xiaomi, Redmi, and Poco'], ['OnePlus', '🔴', 'OnePlus', 'Fast, smooth Android experience'], ['other', '✍️', 'Other brand', 'Enter a brand you prefer'], ['no_preference', '🤝', 'No preference', 'Show the best match from every brand']] },
  { key: 'display', icon: '📱', title: 'How important are display quality and smoothness?', subtitle: 'Choose what you will genuinely notice every day.', why: 'Display quality affects streaming, reading, gaming, and outdoor visibility.', options: [['vivid_gaming', '🎮', 'High refresh rate and vivid colours', '120Hz+, vivid colours, and high brightness for gaming, streaming, and video'], ['reading', '📖', 'Good enough for reading', 'Comfortable scrolling and text; a high refresh rate is not essential'], ['not_priority', '🤷', 'Display is not a priority', 'Anything clear and reliable works for me']] },
  { key: 'storage', icon: '💾', title: 'How do you use storage and memory?', subtitle: 'This weighs both storage needs and fast app switching.', why: 'Heavier use nudges performance and battery higher in your recommendations.', options: [['heavy_user', '📦', 'Heavy user', 'Lots of apps, games, videos, and fast switching between them'], ['light_user', '☁️', 'Light user', 'I rely on cloud storage and clean up regularly'], ['middle', '🎯', 'Somewhere in between', 'I need a balanced, comfortable amount']] },
  { key: 'size', icon: '📐', title: 'What is your ideal phone size and feel?', subtitle: 'A great phone is still wrong if it feels uncomfortable in your hand.', why: 'Size shifts the balance between portability, comfort, and screen space.', options: [['compact', '🤏', 'Compact and light', 'Pocket-friendly and comfortable for one-handed use'], ['big', '🖼️', 'Bigger screen', 'I do not mind weight for more video, work, and game space'], ['doesnt_matter', '🤷', 'No strong preference', 'I am flexible on size and feel']] },
  { key: 'durability', icon: '🛡️', title: 'How much do you value durability and build quality?', subtitle: 'Think about drops, water, commuting, and daily wear.', why: 'This gives more importance to water resistance, materials, and practical design.', options: [['high', '🏗️', 'Very important', 'I am accident-prone or use my phone in tougher environments'], ['somewhat', '⚡', 'Somewhat important', 'Nice to have, but not essential'], ['low', '🤷', 'Not a concern', 'It is not a deciding factor for me']] },
  { key: 'software_longevity', icon: '🗓️', title: 'How long do you keep a phone before upgrading?', subtitle: 'Longer ownership makes battery health and durable design matter more.', why: 'This increases emphasis on battery and design for long-term owners.', options: [['1_2_years', '⚡', '1–2 years', 'I enjoy upgrading for the latest technology'], ['3_4_years', '📅', '3–4 years', 'I want a phone that lasts well'], ['5_plus_years', '🏛️', '5+ years', 'Maximum longevity matters most']] },
  { key: 'current_phone', type: 'text', icon: '📲', title: 'What phone are you using now?', subtitle: 'Optional — we use this only to personalise comparison copy, never the score.', why: 'If we recognise it, results can highlight the upgrades you will actually feel.' },
];

const extraQuestions = [
  { key: 'gaming', icon: '🎮', title: 'How important is gaming performance?', options: [['critical', '🚀', 'Critical', 'BGMI, Genshin, COD Mobile, or other demanding games'], ['casual', '🟡', 'Casual', 'Occasional games and lighter titles'], ['never', '❌', 'Not important', 'I barely game']] },
  { key: 'photography', icon: '📷', title: 'How serious are you about mobile photography?', options: [['professional', '📸', 'Very serious', 'Sharpness, low light, detail, and editing matter'], ['casual', '🌅', 'Casual', 'Social media, stories, reels, and family photos'], ['never', '🙈', 'Rarely', 'Basic snapshots are enough']] },
  { key: 'refresh_rate', icon: '✨', title: 'Do you care about smooth scrolling?', tooltip: 'High refresh rates such as 120Hz make scrolling and animations feel smoother. It is nice to have, but not essential for everyone.', options: [['yes', '✅', 'Yes, 120Hz+ matters', 'I clearly notice the smoother experience'], ['maybe', '🟡', 'Maybe', 'A nice bonus, but not essential'], ['no', '❌', 'No', 'I do not notice or care']] },
  { key: 'ecosystem', icon: '⌚', title: 'Are you invested in a brand ecosystem?', options: [['yes', '✅', 'Yes, ecosystem matters', 'I use matching watches, earbuds, tablets, or computers'], ['somewhat', '🟡', 'Somewhat', 'It is useful, but not critical'], ['no', '🤝', 'No', 'I freely mix and match brands']] },
  { key: 'charging', icon: '⚡', title: 'How much does charging convenience matter?', options: [['critical', '⚡', 'Very important', 'I need 45W+ fast charging or wireless charging'], ['somewhat', '🟡', 'Somewhat', 'Faster is nice, but standard charging is okay'], ['no', '🤷', 'Not important', 'Any charger is fine']] },
  { key: 'software_ui', icon: '🎨', title: 'Which software style do you prefer?', options: [['stock', '📱', 'Stock Android', 'Clean, fast, minimal, and less cluttered'], ['custom', '🎨', 'Customised UI', 'More features and personalisation'], ['doesnt_matter', '🤷', 'No preference', 'Either experience is fine']] },
];

const labels = { budget: { '15k': 'Under ₹15,000', '15_25k': '₹15,000–₹25,000', '25_40k': '₹25,000–₹40,000', '40_70k': '₹40,000–₹70,000', '70k_plus': '₹70,000+' }, size: { compact: 'Compact and light', big: 'Bigger screen', doesnt_matter: 'Flexible on size' }, charging: { critical: 'Fast charging important', somewhat: 'Charging is a nice bonus', no: 'Any charging is fine' }, photography: { professional: 'Photography focus', casual: 'Casual photography', never: 'Basic photography' }, gaming: { critical: 'Gaming focus', casual: 'Casual gaming', never: 'Gaming not important' } };

function OptionCard({ option, selected, onClick, onKeyDown, setRef }) {
  return <button ref={setRef} onClick={onClick} onKeyDown={onKeyDown} className={`answer-card ${selected ? 'selected' : ''}`} style={{ textAlign: 'left', padding: '18px', minHeight: '98px', borderRadius: '15px', border: `1px solid ${selected ? '#60a5fa' : 'var(--border)'}`, background: selected ? 'rgba(37,99,235,0.15)' : 'var(--surface)', color: 'var(--text)', cursor: 'pointer', boxShadow: selected ? '0 0 0 2px rgba(59,130,246,0.16), inset 0 0 16px rgba(59,130,246,0.10)' : 'none', position: 'relative' }}><span style={{ fontSize: '19px', marginRight: '9px' }}>{option[1]}</span><strong style={{ fontSize: '15px' }}>{option[2]}</strong>{selected && <span aria-label="Selected" style={{ position: 'absolute', right: '13px', top: '13px', width: '21px', height: '21px', display: 'grid', placeItems: 'center', borderRadius: '50%', background: 'var(--accent)', color: '#fff', fontSize: '12px', fontWeight: 900 }}>✓</span>}<span style={{ display: 'block', marginTop: '9px', color: 'var(--text3)', fontSize: '12px', lineHeight: 1.45 }}>{option[3]}</span></button>;
}

function ResultCard({ product, rank }) {
  const price = product.bestPrice;
  return <article style={{ background: 'var(--surface)', border: `1px solid ${rank === 0 ? 'var(--accent)' : 'var(--border)'}`, borderRadius: '18px', padding: '18px' }}>{rank === 0 && <div style={{ color: 'var(--accent)', fontWeight: 900, fontSize: '11px', textTransform: 'uppercase' }}>Best match</div>}<div style={{ display: 'flex', gap: '14px', marginTop: '7px' }}>{product.image && <img src={product.image} alt="" style={{ width: '74px', height: '74px', objectFit: 'contain', background: 'rgba(255,255,255,0.04)', borderRadius: '10px' }} />}<div><h3 style={{ margin: 0, fontSize: '16px' }}>{product.name}</h3><strong style={{ color: 'var(--accent)', fontSize: '22px' }}>{product.matched_score}/10</strong><span style={{ color: 'var(--text3)', fontSize: '12px' }}> match</span></div></div><div style={{ marginTop: '14px', display: 'grid', gap: '7px' }}>{Object.entries(product.categoryScores || {}).map(([key, value]) => <div key={key} style={{ display: 'grid', gridTemplateColumns: '86px 1fr 28px', gap: '8px', alignItems: 'center', fontSize: '11px' }}><span style={{ color: 'var(--text3)', textTransform: 'capitalize' }}>{key}</span><span style={{ height: '6px', background: 'var(--border)', borderRadius: '20px', overflow: 'hidden' }}><span style={{ display: 'block', width: `${value * 10}%`, height: '100%', background: 'var(--accent)' }} /></span><strong>{value}</strong></div>)}</div><div style={{ marginTop: '14px', color: 'var(--text2)', fontSize: '12px' }}>{product.top_strengths?.map(s => `✓ ${s.category} ${s.score}/10`).join(' · ')}</div><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginTop: '16px' }}><strong>{price ? `₹${price.toLocaleString('en-IN')}` : 'Price unavailable'}</strong><Link href={`/product/${product.slug}`} style={{ color: 'var(--accent)', fontWeight: 800, fontSize: '12px' }}>View full specs →</Link></div></article>;
}

export default function Quiz() {
  const [phase, setPhase] = useState('core');
  const [step, setStep] = useState(0);
  const [answers, setAnswers] = useState(initialAnswers);
  const [result, setResult] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');
  const [showWhy, setShowWhy] = useState(false);
  const optionRefs = useRef([]);
  const questions = phase === 'extended' ? extraQuestions : coreQuestions;
  const question = questions[step];
  const isText = question.type === 'text';
  const isComplete = isText || Boolean(answers[question.key]);
  const progress = ((step + 1) / questions.length) * 100;
  const remaining = Math.max(1, Math.ceil((questions.length - step - 1) * (phase === 'extended' ? 0.18 : 0.28)));
  const persona = useMemo(() => result?.your_persona?.weight_emphasis?.join(' · '), [result]);
  const confidence = Math.min(96, 58 + Object.values(answers).filter(Boolean).length * 4);

  const update = (key, value) => setAnswers(current => ({ ...current, [key]: value }));
  const fetchResults = async extended => {
    setLoading(true); setError('');
    try { const response = await fetch(`${API_URL}/api/recommend/smart`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...answers, brand_preference: answers.brand_preference === 'other' ? (answers.brand_other || 'no_preference') : answers.brand_preference, extended_set_completed: extended }) }); const data = await response.json(); if (!data.success) throw new Error(data.error || data.message); setResult(data); setPhase(extended ? 'final' : 'preview'); }
    catch (err) { setError(err.message || 'Could not find recommendations.'); }
    finally { setLoading(false); }
  };
  const next = () => { setShowWhy(false); if (step < questions.length - 1) setStep(step + 1); else if (phase === 'extended') setPhase('review'); else fetchResults(false); };
  const previous = () => { setShowWhy(false); if (step) setStep(step - 1); };
  const skip = () => { update(question.key, null); next(); };
  const keyNavigate = (event, index) => { if (event.key === 'ArrowRight' || event.key === 'ArrowDown') { event.preventDefault(); optionRefs.current[(index + 1) % question.options.length]?.focus(); } if (event.key === 'ArrowLeft' || event.key === 'ArrowUp') { event.preventDefault(); optionRefs.current[(index - 1 + question.options.length) % question.options.length]?.focus(); } };
  const summary = [answers.gaming && labels.gaming[answers.gaming], answers.budget && labels.budget[answers.budget], answers.size && labels.size[answers.size], answers.charging && labels.charging[answers.charging], answers.photography && labels.photography[answers.photography]].filter(Boolean);

  if (loading) return <main className="quiz-shell" style={{ display: 'grid', placeItems: 'center', minHeight: '100vh' }}><div style={{ textAlign: 'center', maxWidth: '380px' }}><div className="score-spinner" /><h1>Finding your best matches…</h1><p style={{ color: 'var(--text3)' }}>Scoring stored phones against your profile and hard filters.</p><div style={{ height: '8px', background: 'var(--border)', borderRadius: '20px', overflow: 'hidden', marginTop: '20px' }}><div className="loading-bar" /></div></div><QuizStyles /></main>;

  if (phase === 'review') return <main className="quiz-shell"><div className="quiz-container question-screen"><h1>Ready to see your matches?</h1><p style={{ color: 'var(--text3)' }}>Here’s the profile we will use to rank your five best matches.</p><section className="review-card"><h2>Your profile</h2>{summary.map(item => <div key={item} className="summary-row">✓ {item}</div>)}<div style={{ marginTop: '18px' }}><strong>Profile confidence: {confidence}%</strong><div style={{ height: '7px', background: 'var(--border)', borderRadius: '99px', marginTop: '7px', overflow: 'hidden' }}><div style={{ width: `${confidence}%`, height: '100%', background: 'var(--accent)' }} /></div><span style={{ color: 'var(--text3)', fontSize: '12px' }}>We have enough detail for a focused recommendation.</span></div></section><div className="quiz-actions"><button className="secondary-btn" onClick={() => { setPhase('extended'); setStep(extraQuestions.length - 1); }}>Go back and adjust</button><button className="primary-btn" onClick={() => fetchResults(true)}>Show me my 5 matches →</button></div></div><QuizStyles /></main>;

  if (phase === 'preview' || phase === 'final') return <main className="quiz-shell"><div className="quiz-container question-screen"><h1>Your {phase === 'preview' ? 'best matches' : 'fine-tuned matches'}</h1>{persona && <p style={{ color: 'var(--text3)' }}>Your profile: {persona}</p>}{error ? <><p style={{ color: '#f87171' }}>{error}</p><button className="secondary-btn" onClick={() => { setPhase('core'); setStep(0); }}>Adjust answers</button></> : <div style={{ display: 'grid', gap: '14px', marginTop: '24px' }}>{result?.top_picks?.map((product, index) => <ResultCard key={product._id} product={product} rank={index} />)}</div>}{phase === 'preview' && !error && <div className="quiz-actions"><button className="primary-btn" onClick={() => { setPhase('extended'); setStep(0); }}>Fine-tune for a perfect match →</button><button className="secondary-btn" onClick={() => fetchResults(true)}>I’m happy with these</button></div>}{phase === 'final' && <button className="secondary-btn" style={{ marginTop: '24px' }} onClick={() => { setPhase('core'); setStep(0); setResult(null); }}>Retake quiz</button>}</div><QuizStyles /></main>;

  optionRefs.current = [];
  return <main className="quiz-shell"><div className="quiz-container question-screen"><div className="progress-meta"><span>{phase === 'core' ? '⏱️ 2–3 minutes' : 'Optional fine-tuning'} · {phase === 'core' ? `Step ${step + 1} of ${coreQuestions.length}` : `Fine-tuning ${step + 1} of ${extraQuestions.length}`}</span>{step > 0 && <button onClick={previous}>← Back</button>}</div><div className="progress-track"><div style={{ width: `${progress}%` }} /></div><div className="progress-caption">{remaining} min left · <strong>{phase === 'core' ? 'Fine-tuning is optional' : 'You can skip any question'}</strong></div><p className="keyboard-tip">💡 Tip: use arrow keys to move between answers, then Enter or Space to choose.</p><div className="question-heading"><span>{question.icon}</span><h1>{question.title}{question.tooltip && <span className="tooltip" tabIndex="0">? <small>{question.tooltip}</small></span>}</h1></div>{question.subtitle && <p className="question-subtitle">{question.subtitle}</p>}{question.why && <button className="why-btn" onClick={() => setShowWhy(!showWhy)}>ℹ Why we ask {showWhy ? '−' : '+'}</button>}{showWhy && <p className="why-copy">{question.why}</p>}{isText ? <input autoFocus value={answers.current_phone} onChange={event => update('current_phone', event.target.value)} placeholder="For example: iPhone 13" className="text-input" /> : <div className="option-grid">{question.options.map((option, index) => <OptionCard key={option[0]} option={option} selected={answers[question.key] === option[0]} onClick={() => update(question.key, option[0])} onKeyDown={event => keyNavigate(event, index)} setRef={node => { optionRefs.current[index] = node; }} />)}</div>}{question.key === 'brand_preference' && answers.brand_preference === 'other' && <input value={answers.brand_other} onChange={event => update('brand_other', event.target.value)} placeholder="Type a brand name" className="text-input" />}<div className="quiz-actions sticky-actions">{phase === 'extended' && !isText && <button className="secondary-btn" onClick={skip}>Skip this question</button>}<button disabled={!isComplete && !isText} onClick={next} className="primary-btn">{isText && !answers.current_phone ? 'Skip →' : step === questions.length - 1 ? phase === 'extended' ? 'Review my profile →' : 'Show me my matches →' : 'Continue →'}</button></div></div><QuizStyles /></main>;
}

function QuizStyles() { return <style jsx global>{`
  .quiz-shell{min-height:100vh;background:var(--bg);color:var(--text);padding:96px 20px 40px}.quiz-container{max-width:720px;margin:0 auto}.question-screen{animation:fadeInUp .28s ease-out}.progress-meta{display:flex;justify-content:space-between;gap:12px;font-size:13px;color:var(--text3)}.progress-meta button,.why-btn{background:transparent;border:0;color:var(--accent);cursor:pointer;font-weight:800}.progress-track{height:8px;background:var(--border);border-radius:99px;overflow:hidden;margin-top:10px}.progress-track>div{height:100%;background:linear-gradient(90deg,var(--accent),#60a5fa);transition:width .25s ease}.progress-caption,.keyboard-tip{color:var(--text3);font-size:12px;margin:9px 0}.keyboard-tip{padding:9px 11px;background:rgba(59,130,246,.08);border-radius:8px;width:max-content}.question-heading{display:flex;gap:12px;align-items:flex-start;margin-top:32px}.question-heading>span{font-size:27px}.question-heading h1{font-size:30px;line-height:1.22;margin:0}.question-subtitle{color:var(--text3);line-height:1.55;margin:10px 0}.why-copy{color:var(--text2);font-size:13px;line-height:1.5;margin:9px 0 0;padding:10px 12px;border-left:2px solid var(--accent);background:rgba(59,130,246,.06)}.option-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:24px}.answer-card{transition:all .18s ease-out}.answer-card:hover:not(.selected){border-color:rgba(96,165,250,.75)!important;background:rgba(59,130,246,.06)!important;transform:translateY(-2px);box-shadow:0 8px 20px rgba(0,0,0,.12)}.answer-card:active{transform:scale(.98)}.answer-card:focus-visible{outline:3px solid rgba(96,165,250,.6);outline-offset:2px}.answer-card.selected{animation:slideInCheck .3s ease-out}.text-input{width:100%;box-sizing:border-box;margin-top:24px;padding:16px;border-radius:12px;border:1px solid var(--border);background:var(--surface);color:var(--text);font-size:16px}.quiz-actions{display:flex;gap:12px;justify-content:flex-end;flex-wrap:wrap;margin-top:30px}.primary-btn,.secondary-btn{padding:13px 19px;border-radius:10px;font-weight:900;cursor:pointer;font-size:13px}.primary-btn{border:0;background:linear-gradient(135deg,var(--accent),#2563eb);color:#fff;box-shadow:0 7px 18px rgba(37,99,235,.25)}.primary-btn:disabled{background:var(--border);box-shadow:none;cursor:not-allowed}.secondary-btn{border:1px solid var(--border);background:transparent;color:var(--text2)}.review-card{margin-top:24px;padding:20px;border:1px solid var(--border);background:var(--surface);border-radius:16px}.review-card h2{font-size:15px;margin-top:0}.summary-row{padding:8px 0;color:var(--text2);border-bottom:1px solid var(--border)}.tooltip{display:inline-grid;place-items:center;width:18px;height:18px;border:1px solid var(--text3);border-radius:50%;font-size:12px;color:var(--text3);margin-left:8px;vertical-align:middle;position:relative;cursor:help}.tooltip small{display:none;position:absolute;width:230px;z-index:4;left:0;top:25px;padding:10px;background:var(--surface);border:1px solid var(--border);border-radius:8px;color:var(--text2);font-size:11px;line-height:1.45;font-weight:400}.tooltip:hover small,.tooltip:focus small{display:block}.score-spinner{width:42px;height:42px;margin:0 auto 18px;border:4px solid var(--border);border-top-color:var(--accent);border-radius:50%;animation:spin .75s linear infinite}.loading-bar{width:70%;height:100%;background:var(--accent);animation:loading 1.1s ease-in-out infinite}.sticky-actions{padding-bottom:8px}@keyframes fadeInUp{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}@keyframes slideInCheck{from{transform:scale(.98)}to{transform:scale(1)}}@keyframes spin{to{transform:rotate(360deg)}}@keyframes loading{0%{transform:translateX(-100%)}100%{transform:translateX(160%)}}@media(max-width:640px){.quiz-shell{padding:78px 16px 92px}.question-heading h1{font-size:23px;line-height:1.35}.option-grid{grid-template-columns:1fr}.answer-card{min-height:88px}.keyboard-tip{width:auto;line-height:1.45}.sticky-actions{position:sticky;bottom:0;background:var(--bg);padding:14px 0 2px;margin-top:20px}.sticky-actions .primary-btn{flex:1}.progress-meta{font-size:13px}.question-heading{margin-top:24px}}
`}</style>; }
